import {Circle} from "@mui/icons-material";
import {Box} from "@mui/material";

export default function TimelineKeyframeIcon() {
    return (
        <Box
            sx={{
                backgroundColor: "#206b95",
                padding: "0 2px",
                borderRadius: 1
            }}
        >
            <Circle
                sx={{fontSize: 10}}
            />

        </Box>
    );
}